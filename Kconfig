config VIDEO_ARDUCAM_MEGA
    bool                  # ← Critical: no prompt string (hidden symbol)
    default y             # ← Unconditional — always on when module is included
    select VIDEO
    select SPI
    help
      Arducam Mega SPI camera driver (off-tree module).
      Automatically enables the VIDEO and SPI subsystems.
      The driver will only function if a compatible devicetree node
      (compatible = "arducam,mega"; status = "okay";) is present.
