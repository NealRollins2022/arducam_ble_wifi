# SPDX-License-Identifier: Apache-2.0
description: Arducam Nord SPI camera

compatible: "arducam,nord"

include: spi-device.yaml

properties:
  spi-max-frequency:
    type: int
    required: true   # or false if you want it optional


